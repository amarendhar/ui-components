import { css } from 'styled-components'

const Focus = css`
  outline: none;
  // ToDo: Move outlineColor to themes
  outline-color: rgb(77, 144, 254);
  outline-style: auto;
  outline-offset: 4px;
`

export default Focus
